import RegisterForm from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Create Account — Petal & Bloom",
  description: "Create your Petal & Bloom account to start shopping.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
