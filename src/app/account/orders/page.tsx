import OrderHistory from "@/components/account/OrderHistory";

export const metadata = {
  title: "Order History — Petal & Bloom",
  description: "View your order history and track shipments.",
};

export default function OrdersPage() {
  return <OrderHistory />;
}
