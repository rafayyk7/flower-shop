import LoginForm from "@/components/auth/LoginForm";

export const metadata = {
  title: "Sign In — Petal & Bloom",
  description: "Sign in to your Petal & Bloom account.",
};

export default function LoginPage() {
  return <LoginForm />;
}
